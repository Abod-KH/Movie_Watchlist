@model Movie_Watchlist.Application.ViewModels.MovieDetailsViewModel
@{
    ViewData["Title"] = Model.Movie!.Title;
}

<div class="container mt-5">

    
    <div class="card shadow-lg border-0 mb-5">
        <div class="row g-0">

          
            <div class="col-lg-4">
                <img src="@Model.Movie.PosterPath"
                     class="img-fluid h-100 w-100"
                     style="object-fit: cover;">
            </div>

            
            <div class="col-lg-8">
                <div class="card-body p-4 p-lg-5">

                    <h1 class="fw-bold">@Model.Movie.Title</h1>
                    <p class="text-muted">
                        @Model.Movie.ReleaseYear • @Model.Movie.Genre?.Name
                    </p>

                    <hr />

                    <h5 class="fw-bold">Overview</h5>
                    <p class="text-secondary">
                        @Model.Movie.Description
                    </p>

                   
                    @if (!string.IsNullOrEmpty(Model.TrailerKey))
                    {
                        <div class="mt-4">
                            <div class="ratio ratio-16x9">
                                <iframe src="https://www.youtube.com/embed/@Model.TrailerKey"
                                        allowfullscreen></iframe>
                            </div>
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>

    
    <div class="mt-5">
        <h3 class="fw-bold mb-4">More Like This</h3>

        <div class="row row-cols-2 row-cols-md-4 row-cols-lg-6 g-4">
            @foreach (var sim in Model.SimilarMovies ?? Enumerable.Empty<MovieApiResult>())
            {
                <div class="col">
                    <div class="card h-100 border-0 shadow-sm">

                        <a asp-action="DetailsByTmdb" asp-route-tmdbId="@sim.Id">
                            <img src="@sim.FullPosterPath"
                                 class="card-img-top"
                                 style="height: 250px; object-fit: cover;">
                        </a>

                        <div class="card-body p-2">
                            <p class="small fw-semibold text-truncate mb-0">
                                @sim.Title
                            </p>
                        </div>

                    </div>
                </div>
            }
        </div>
    </div>

</div>


